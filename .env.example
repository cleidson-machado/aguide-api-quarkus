# ========================================
# VARIÁVEIS DE AMBIENTE - TEMPLATE
# ========================================
# COPIE ESTE ARQUIVO PARA .env E CONFIGURE SUAS CREDENCIAIS
# Comando: cp .env.example .env
#
# ⚠️  NUNCA COMITE O ARQUIVO .env NO GIT!
# ========================================

# ========== AMBIENTE ==========
# dev   = Desenvolvimento local (usa quarkus_dev, permite clean-at-start)
# prod  = Produção VPS (usa quarkus_db, PROTEGIDO contra reset)
# test  = Testes (usa quarkus_test, limpa antes de cada teste)
QUARKUS_PROFILE=dev

# ========== DATABASE - PRODUÇÃO ==========
# ⚠️  CRÍTICO: Este banco NUNCA deve ser limpo!
# Usado APENAS em produção (VPS com Docker Compose)
DB_PROD_HOST=your_postgres_host
DB_PROD_PORT=5432
DB_PROD_NAME=your_production_database
DB_PROD_USERNAME=your_db_user
DB_PROD_PASSWORD=your_secure_password_here

# ========== DATABASE - DESENVOLVIMENTO ==========
# Banco isolado para desenvolvimento local
# Pode ser limpo/resetado sem risco de perder dados de produção
DB_DEV_HOST=localhost
DB_DEV_PORT=5432
DB_DEV_NAME=your_dev_database
DB_DEV_USERNAME=your_db_user
DB_DEV_PASSWORD=your_dev_password_here

# ========== DATABASE - TESTES ==========
# Banco dedicado para testes
# É limpo antes de cada execução de testes (Flyway clean-at-start=true)
DB_TEST_HOST=localhost
DB_TEST_PORT=5432
DB_TEST_NAME=your_test_database
DB_TEST_USERNAME=your_db_user
DB_TEST_PASSWORD=your_test_password_here

# ========== JWT ==========
# Tempo de expiração do token JWT em segundos
# 3600 = 1 hora | 7200 = 2 horas | 86400 = 24 horas
JWT_EXPIRATION_TIME=3600

# ========== HTTP/HTTPS ==========
# Porta HTTP (produção usa 8083, dev pode usar 8083 também)
QUARKUS_HTTP_PORT=8083

# Porta HTTPS (apenas desenvolvimento local)
QUARKUS_HTTPS_PORT=8443

# ========== LOGS ==========
# DEBUG = desenvolvimento | INFO = produção
QUARKUS_LOG_LEVEL=DEBUG
